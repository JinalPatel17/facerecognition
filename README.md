# SmartBrain (Base Version)
The API and react integreted project for detecting face in given link.
API from Clarifai which trained to detect face with machine learning used in this project.

1. Run `npm install`
2. Run `npm start`
